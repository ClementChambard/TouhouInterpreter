#ifndef THECL_FORMAT_H_
#define THECL_FORMAT_H_

#include <string>

const std::string thecl_find_format(unsigned int version,
                            unsigned int id, bool is_timeline);

#endif // THECL_FORMAT_H_
